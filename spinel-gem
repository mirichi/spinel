#!/bin/sh
# spinel-gem (POC) — wrapper that calls the Ruby implementation
DIR="$(cd "$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")" && pwd)"
exec ruby "$DIR/spinel_gem.rb" "$@"
